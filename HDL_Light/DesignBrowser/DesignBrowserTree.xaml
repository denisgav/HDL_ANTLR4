<UserControl x:Class="Schematix.DesignBrowser.DesignBrowserTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <!--<RowDefinition Height="Auto"></RowDefinition>-->
        </Grid.RowDefinitions>

        <Grid Margin="2" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button x:Name="ButtomUpdate" Grid.Column="2" Margin="2" Click="ButtomUpdate_Click">Update</Button>
        </Grid>

        <TreeView 
            Grid.Row="1" 
            x:Name="TreeViewDesignBrowser">
        </TreeView>

        <!--<StatusBar Grid.Row="2" x:Name="StatusBar1">Text of status bar</StatusBar>-->
    </Grid>
</UserControl>
