<UserControl x:Class="Schematix.Waveform.UserControls.TimeMeasureDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="80"
             d:DesignHeight="80" d:DesignWidth="500"
             Loaded="UserControl_Loaded">
    
    <UserControl.Resources>
        <ContextMenu x:Key="ContextMenuArrow">
            <MenuItem x:Name="MenuItemDelete" Header="Delete" Click="MenuItemDelete_Click">
                <MenuItem.Icon>
                    <Image Source="/Schematix.Waveform;component/icons/Delete.jpg" Width="16" Height="16"/>
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>    
    </UserControl.Resources>
    
    <Canvas x:Name="MainCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></Canvas>
</UserControl>
